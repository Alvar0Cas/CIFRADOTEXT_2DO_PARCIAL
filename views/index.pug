doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/css/styles.css')
    meta(name="viewport", content="width=device-width, initial-scale=1.0")

  body
    include nav-bar

    .feedback-card
      .feedback-header CIFRADOR
      form.feedback-body
        input.feedback-body__email(placeholder="Email", type="email")
        textarea.feedback-body__message(placeholder="Message", type="text")
        button.feedback-body__submit SEND
        script.
          (function () {
            var btn = document.getElementsByClassName("feedback-body__submit")[0];
            btn.onclick = function(e) {
              e.preventDefault();
            };
          })()
