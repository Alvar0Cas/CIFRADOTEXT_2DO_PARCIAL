const express = require('express');
const router = express.Router();

// Importación para Base64
const base64 = require('base-64');

// Importación para el cifrado César
const caesarCipher = require('caesar-cipher');

// Importación para el cifrado de sustitución
const substitutionCipher = require('substitution-cipher');

// Importación para la codificación binaria
const binaryEncoding = require('binary-encoding');
